"""Snowflake backend usage examples.

This package contains example implementations of named connections,
named queries, and named procedures for the Snowflake backend.

Named Connection: Python callable returning SnowflakeConnectionConfig
Named Query: Python callable taking (dialect, ...) returning BaseExpression
Named Procedure: Class inheriting Procedure implementing run(ctx)
"""
